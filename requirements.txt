# --- Core scientific & image libs
numpy>=1.24,<2.0
pandas>=2.0,<2.2
Pillow>=9.5,<10.0

# OpenCV: headless variant for servers
opencv-python-headless>=4.7.0,<5.0

scikit-image>=0.20.0
scipy>=1.11.0

# --- OCR
pytesseract>=0.3.11

# --- PaddlePaddle + PaddleOCR (CPU wheels)
# Install paddlepaddle first via Docker RUN, so comment here to avoid pip source build
# paddlepaddle==3.2.0
# paddleocr>=3.3.0

# --- Google Vision OCR
google-cloud-vision>=3.5.0

# --- PDF Handling
pdf2image>=1.16.3
PyPDF2>=3.0.0

# --- Table Detection / YOLO (Ultralytics)
ultralytics>=8.0.0

# --- ML Helpers
rapidfuzz>=2.14.0

# --- API / Backend
fastapi>=0.95.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# --- HTTP & Utilities
requests>=2.31.0
tqdm>=4.65.0

# --- LLM Integration
openai>=1.8.0

# --- Image augmentation (optional)
imgaug>=0.4.0

# --- Dev / Utilities
python-dotenv>=1.0.0
